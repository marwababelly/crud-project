import React from "react";
import style from './List.module.css';

const List = () => {
    return (
        <section className={style.list}>
            <h2>List</h2>
        </section>
    );
};

export default List;